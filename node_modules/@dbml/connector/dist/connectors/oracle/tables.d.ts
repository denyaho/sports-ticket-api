import { Connection } from 'oracledb';
import { FieldsDictionary, Table } from '../types';
export declare function generateTablesAndFields(client: Connection): Promise<{
    tables: Table[];
    fields: FieldsDictionary;
}>;
export declare function getFieldType(dataType: string, characterMaximumLength: number, numericPrecision: number | null, numericScale: number | null): string;
