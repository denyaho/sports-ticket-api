import { Connection } from 'oracledb';
import { CheckConstraintDictionary, IndexesDictionary, TableConstraintsDictionary } from '../types';
export declare function generateConstraints(client: Connection): Promise<{
    indexes: IndexesDictionary;
    tableConstraints: TableConstraintsDictionary;
    checks: CheckConstraintDictionary;
}>;
