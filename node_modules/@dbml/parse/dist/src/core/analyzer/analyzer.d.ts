import { ProgramNode } from '../types/nodes';
import { default as Report } from '../types/report';
import { NodeSymbolIdGenerator } from '../types/symbol/symbols';
export default class Analyzer {
    private ast;
    private symbolFactory;
    constructor(ast: ProgramNode, symbolIdGenerator: NodeSymbolIdGenerator);
    analyze(): Report<ProgramNode>;
    validate(): Report<ProgramNode>;
}
