import { CompileError } from '../../../types/errors';
import { default as SymbolFactory } from '../../../types/symbol/factory';
import { ElementDeclarationNode, ProgramNode } from '../../../types/nodes';
import { SyntaxToken } from '../../../types/tokens';
import { ElementBinder } from '../types';
export default class TableGroupBinder implements ElementBinder {
    private symbolFactory;
    private declarationNode;
    private ast;
    constructor(declarationNode: ElementDeclarationNode & {
        type: SyntaxToken;
    }, ast: ProgramNode, symbolFactory: SymbolFactory);
    bind(): CompileError[];
    private bindBody;
    private bindFields;
    private bindSubElements;
}
