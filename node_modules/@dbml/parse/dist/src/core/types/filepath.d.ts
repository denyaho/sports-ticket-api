import { Internable } from './internable';
declare const __filepathIdBrand: unique symbol;
export type FilepathId = string & {
    [__filepathIdBrand]: true;
};
export declare class Filepath implements Internable<FilepathId> {
    private readonly path;
    constructor(absolutePath: string);
    intern(): FilepathId;
    static from(absolutePath: string): Filepath;
    static resolve(fromDir: string, relativePath: string): Filepath;
    static fromUri(uri: string): Filepath;
    get absolute(): string;
    get dirname(): string;
    get basename(): string;
    get extname(): string;
    resolve(relativePath: string): Filepath;
    join(...segments: string[]): Filepath;
    relativeTo(baseDir: string): string;
    toString(): string;
    equals(other: Filepath): boolean;
    toUri(): string;
    static isRelative(p: string): boolean;
}
export declare function resolveImportFilepath(currentFilepath: Filepath, relativePath: string): Filepath | undefined;
export {};
