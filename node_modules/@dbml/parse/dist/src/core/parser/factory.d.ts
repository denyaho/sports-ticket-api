import { Filepath } from '../types/filepath';
import { SyntaxNode, SyntaxNodeId, SyntaxNodeIdGenerator } from '../types/nodes';
export default class NodeFactory {
    private generator;
    private filepath;
    constructor(generator: SyntaxNodeIdGenerator, filepath: Filepath);
    create<T extends SyntaxNode, A>(Type: {
        new (args: A, id: SyntaxNodeId, filepath: Filepath): T;
    }, args: A): T;
}
