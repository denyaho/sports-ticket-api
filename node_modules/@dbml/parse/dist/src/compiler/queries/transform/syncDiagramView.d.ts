import { TextEdit } from './applyTextEdits';
export interface DiagramViewSyncOperation {
    operation: 'create' | 'update' | 'delete';
    name: string;
    newName?: string;
    visibleEntities?: {
        tables?: Array<{
            name: string;
            schemaName: string;
        }> | null;
        stickyNotes?: Array<{
            name: string;
        }> | null;
        tableGroups?: Array<{
            name: string;
        }> | null;
        schemas?: Array<{
            name: string;
        }> | null;
    };
}
export interface DiagramViewBlock {
    name: string;
    startIndex: number;
    endIndex: number;
}
export declare function findDiagramViewBlocks(source: string): DiagramViewBlock[];
/**
 * Synchronizes DiagramView blocks in DBML source code.
 *
 * @param dbml - The original DBML source code
 * @param operations - Array of operations to apply (create, update, delete)
 * @param blocks - Optional pre-parsed blocks from findDiagramViewBlocks. If not provided, parses internally.
 * @returns Object containing the new DBML source code and the text edits applied
 */
export declare function syncDiagramView(dbml: string, operations: DiagramViewSyncOperation[], blocks?: DiagramViewBlock[]): {
    newDbml: string;
    edits: TextEdit[];
};
